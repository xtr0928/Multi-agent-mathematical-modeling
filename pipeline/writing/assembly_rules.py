# -*- coding: utf-8 -*-
"""assembly_rules.py — S5 装配层规范（v5.2.1 新增，2026-08-16 华数杯 C 教训）

装配层自由度收敛：数字由注册表注入（numbers_policy），但"结论怎么从数字推导出来"
此前无人管——迁移价值的反复圆说、头条归因层级、命题拔高，都源于写作层可以绕开
对照表的字面结论自由组织语言。本规范把三条规则写成装配前必读 + 装配后必查的清单。
"""

# 装配前必读（S5 写入提示词）
RULES = [
    'R1 对照表→结论直接导出：每个比较级结论必须由注册表中某个对照表直接导出；'
    '对照表的字面结论（如"迁移时延 29.8ms 劣于本地 5.0ms"）不得被反向圆说成正面价值，'
    '只能如实表述"无显著增量价值"或给出表中未出现的新证据。',
    'R2 归因层级：头条结论（如"BAU 18.02 亿 → ≈0"）的结构性原因（弃置新能源的口径差）'
    '必须与结论同级呈现，不得放在"须如实说明"补充句；摘要层级=结论+前提同句。',
    'R3 声明强度上限：定义恒等式不得包装为命题/定理；"充分必要"级声明必须能写出'
    '双向证明；敏感性结果不得表述为普适规律。',
    'R4 可疑精确性必附机制：σ=0、末态=初态逐位相同、命中率 1.000 等"完美结果"'
    '必须注明由哪条约束/哪项口径导致，否则禁止入文。',
    'R5 方法对比完备性：被排除的方法（LSTM 等）须写明实证或数据形状理由；'
    '建模粒度（分组建模/合并建模）的选择理由必须与数据特征画像（零值占比/周期强度）挂钩。',
    'R6 指标报告体例统一：同一指标全表取值口径一致，不允许一处有值一处空。',
]

# 装配后必查（确定性脚本 + LLM 各查一半）
CHECKLIST = {
    "对照表导出": "每个'本文方案优于/劣于'句 → 注册表对照表行是否支撑字面结论",
    "归因层级": "摘要与结论节的头条数字是否同句携带结构性前提",
    "声明强度": "全文'命题/定理/充分必要'清单 → 逐条核查数学实质",
    "可疑完美": "全文小数位数为 0/整数巧合的数字清单 → 逐条核查机制说明",
    "基线完备": "预测/调度/MCDA 各节的方法对比清单 → 是否有数据形状适配的替代基线",
    "体例统一": "同名指标跨表取值口径 diff",
}

if __name__ == "__main__":
    print("S5 装配规范（v5.2.1）：")
    for r in RULES:
        print(" ", r)
    print("\n装配后检查项：")
    for k, v in CHECKLIST.items():
        print(f"  [{k}] {v}")
