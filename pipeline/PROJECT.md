# 数模管线 V6.1 · 项目文档

> 本目录是可执行管线实现。设计依据：`../docs/V5_pipeline_design.md`（v5.1）+ `../docs/V52_pipeline_design.md`（v5.2 定稿）
> + `../docs/V61_antipoison_design.md`（v6.1 反投毒）。
> v5.2 触发：v5.1 产物经 v2.8 评审 47/100（成功参赛奖档）——根因是"过程治理 ≠ 产物正确；四模型商议 ≠ 有人负责"。
> v5.2 三反转：**建模时间前置 · 方案所有权收拢（单一拍板者）· 产物分数进门禁**。
> V6 设计（2026-09-19 修订）：`../docs/V6_per_question_pipeline_dev_doc_2026-09-13.md` + `../docs/V6_flowchart.html`——逐问推进重构，M1 待开工；新增代码落 `pipeline/v6/`。
> **v6.1 增量（已落地 2026-09-25）**：**S-1 反投毒摄入门禁**——所有外部输入（题目 PDF / 数据集 / 图片）
> 必须先过安检。核心命题：**文本层 ≠ 可见内容**（PDF 允许文字存在但不渲染，或用与背景同色；
> pdftotext / pymupdf 会原样读出隐藏指令）。8 类 30 项确定性检测，零 LLM 成本。
> 关系说明：V6.1 反投毒是**独立模块**，不依赖逐问推进——V6 的 P1（交接包）落地时可直接复用本门禁。

## 项目定位

数学建模竞赛（MCM/ICM/华数杯）72 小时解题管线的**确定性基础设施**。LLM 负责推理与写作，本目录负责**数据正确性铁律**：

1. **所有操作基于正确的数据**（用户指令 2026-08-15）：任何上游修改（数据/脚本/参数）→ 下游相关数据**全部作废重算**（工作区 manifest.json + freshness.py 强制执行：内容哈希指纹→传递闭包 stale→拓扑序自动重跑；哈希口径 V6 起统一 SHA-256，M1 由 SHA-512 切换）
2. **官方模板优先**（用户指令 2026-08-15）：S5 排版**先搜索官方模板**再动笔；TemplateGate 华数杯 10 项/MCM 6 项清单逐条核对，缺清单直接阻断
3. **篇幅达标**（用户指令 2026-08-15）：正文页数 ≥ 竞赛上限 ×75%（华数杯 20 页 → ≥15 页），LengthGate 低于即阻断；补内容=公式推导/表格/灵敏度/附录，非注水
4. **防漂移借内容哈希**（用户指令；V6 口径统一 SHA-256）：三级新鲜度链，防错误数据复用，保证数据正确且唯一
5. **数值验证必须，保证所有数据可复现**（用户指令）：四断言 + 可复现性断言，每个数字可由「脚本+输入哈希+参数」重跑还原
6. **发挥算力优势**（用户指令）：C++ OpenMP 确定性重算，128 核服务器；旧进程不杀并行对照
7. **排版直观**（用户指令）：图/公式清单 + O 奖语料密度基线 + 直观性检查，排版检查交 Qwen
8. **外部输入必须先过反投毒门**（用户指令 2026-09-25）：题目/数据集/图片在进入 S0 之前，
   必须经 S-1 门禁检测物理隐藏投毒（白字/不可见渲染/离屏/注释/元数据/附件/图片内隐藏文字…），
   产出净化版题目 + 投毒报告；有 HIGH 级发现则 fail-closed 阻断，须人工确认后放行

## 目录结构

```
pipeline/
├── engine/                     # 阶段2：作废重算引擎（核心）
│   ├── hashing.py              #   哈希工具（V6 口径统一 SHA-256，M1 由 SHA-512 切换）：语义化参数哈希(分作用域)/文件哈希/组合哈希/env_hash
│   ├── dag.py                  #   六类节点依赖图：环检测/拓扑序/传递闭包(A1/A2)
│   ├── registry.py             #   claims_registry：五元组绑定/写入门卫(A4)/三级新鲜度/原子发布(A16)
│   └── invalidation.py         #   作废传播引擎：哈希扫描→传递闭包作废→拓扑序重算调度(A5)
├── cpp/                        # 阶段3：C++ OpenMP 执行器
│   ├── executor.py             #   文件队列(pending/running/done/failed)/编译缓存/确定性校验/Python回退
│   ├── ks_compare.py           #   跨语言对照：预生成 RNG 文件协议 + KS 分布比较（D15）
│   └── templates/hot_loop_template.cpp  # 确定性重算模板：固定分块+pairwise归约+counter-based RNG(线程数无关)
├── gate/numeric_gate.py        # 阶段4：数值验证门禁（四断言+可复现性断言+build钩子，A25/A26/A32）
├── state/state_machine.py      # 阶段5：双状态机（断点恢复+append-only日志+72h时间窗+预授权降级）
├── assumptions/                # 阶段6：假设官
│   ├── officer.py              #   生命周期四步/三级分类/作者回避强制/隐式假设检查(A17/A18)
│   └── implicit_map.py         #   方法→隐式假设映射表（20 种常用方法）
├── layout/layout_gate.py       # 阶段7：排版层（图清单/公式清单/四维密度/直观性规则，A21/A22）
├── baseline/                   # 阶段1：O 奖语料基线
│   ├── corpus_baseline.py      #   PDF 语料图/公式/表密度 IQR 统计（A20：≥30 篇）
│   └── baseline.json           #   统计产物（本仓库以默认值先验启动，跑语料后覆盖）
├── visual/dag_visualizer.py    # 阶段8：DAG 可视化（registry 依赖自动生成，A24 禁止手绘）
├── writing/template.py         # 阶段8：微单元写作模板（段落绑定 claim_id，stale 引用阻断 A6/A19）
├── audit/role_audit.py         # 阶段8：角色隔离审计（写评分离/作者回避/零上下文，A28）
├── reflect/                    # 阶段8 补全：反思器 + Devil's Advocate
│   ├── reflector.py            #   完成度检查（任务清单vs产物）+报错结构化反思+hot_loop 推荐，≤3 轮
│   └── devils_advocate.py      #   DA 独立角色：攻击清单（反事实+实验设计强制，实质≥2，关键全覆盖）
├── seed/seed_library.py        # 阶段9：种子库（华数杯 17 条结构化 + 检出器，A23）
├── bench/internal_benchmark.py # 阶段9：内部基准反馈环（C++ 收益/假设官失职/predicate 覆盖率）
├── ingest/                     # V6.1：S-1 反投毒摄入门禁（零 LLM，确定性）
│   ├── poison_detector.py      #   8 类 30 项检测器（视觉/载体/字符/结构/图片/数据集/语义）
│   └── anti_poison_gate.py     #   门禁封装 + 三件套产出 + SHA-512 哈希链（fail-closed）
├── orchestrator.py             # LLM 编排层：完整 S0-S6（双轨迹选择+投票+检测层作者回避；DryRun 零成本）
└── tests/
    ├── test_engine.py          #   核心引擎验收（A1/A2/A4/A5/A14/A16）
    ├── test_stages.py          #   阶段3-8 验收（A13/A16/A17/A18/A22/A26/时间窗）
    ├── test_remaining.py       #   剩余验收（A3/A14/A15/A19/A23/A24/A28/编排层/基准）
    ├── test_completion.py      #   补全验收（反思器/DA/KS/完整编排层：双轨迹+检测层作者回避）
    ├── test_anti_poison.py     #   V6.1 反投毒门禁验收（G1-G8，26 项）
    └── fixtures/attack/        #   16 种攻击手法样本 + 正常对照（防回归）
```

## 总体架构（v5.2 完整 S0-S7，设计文档 V52_pipeline_design.md 全落位）

```
S-1 反投毒摄入门禁（V6.1 ★：题目/数据集/图片 → 净化版 + 投毒报告；0 LLM，fail-closed）
  → S0 问题解析 → S1 参谋组(四模型只交候选方案，不产定稿) + 分歧清单
  → S1.5 总设计定稿（单一拍板者 ★）
        自动 = 新开 DeepSeek 实例（零上下文防路径固执）
        半自动 = 问博士拍板
  → S2 按定稿求解（定稿即法律，不许自创方法/参数/口径）
  → S3 四模型评审 → S3 投票 → S4 数值门禁(0 LLM，确定性)
  → S5 排版(视觉官) → S5b 检测层作者回避抽取
  → S6 自动修复回路（修订单→repair→再评审，直到收敛）
  → S7 产物分数回归门（v2.8 评审分数 ≥ 基线，否则阻断交付）
```

完整场次 LLM 调用预估 **30 次**（S0:1 + S1:4参谋+1分歧+1假设官+1DA + S1.5:1拍板 + S2:2 + S3:4+1 + S4:0 + S5:1 + S5b:12 + S6:0确定性 + S7:0确定性 + S6交付:1）。
整体架构图：`../docs/V52_overall_arch.html`（纸墨风 9 泳道版）。设计文档：`../docs/V52_pipeline_design.md`。

## V6 逐问推进管线（设计修订中 · M1 待开工）

设计：`../docs/V6_per_question_pipeline_dev_doc_2026-09-13.md`（v6 修订）｜ 流程图：`../docs/V6_flowchart.html`（同名 .png）

- 形态：**逐问串行**（Q1..QN：P1 交接包 → P2 独立推理 → P3 分歧＋假设审计 → P4 主模型统一 → P5 求解 → P6 门禁 → P7 评审＋评审长 → P8 修复回路 → P9 冻结）；终局：跨问一致性 → 总评审 → 排版 → 检测层 → 交付。
- 落位：新增代码 `pipeline/v6/`（入口 `pipeline/run_v6.py`）；本目录的 engine/gate/layout/assumptions 等作为确定性地基复用（hashing 算法随 M1 统一 SHA-256）。
- 与本目录直接相关的变化：
  1. **调用层**：生产后端 = `llm_bridge.py` → 兄弟仓库 `llm_client.ask()`（deepseek/dsflash/glm/kimi/qwen 五 provider）；`models.json` 为 M1 新建别名层。
  2. **哈希口径**：全系统统一 **SHA-256**（与 git 对象格式同族）；v5 旧产物不进 v6 校验链。
  3. **点将**：先问后验——每次开始建模先问博士（用几个/哪些模型），问完只对所选别名做真调用核查、状态表裁决。
  4. **文件管理**：一个 run = 一个目录 = 一个独立 git 仓库（`runs/<RUN_ID>/`；外层 `.gitignore` 排除 `runs/`）。

## 核心机制速览

### 1. 作废重算引擎（数据正确性铁律的实现）

```
param → input → script → result → claim → paragraph（六类节点 DAG）
   │        │        │
   └────────┴────────┴─ 任一哈希变化 → 传递闭包下游全部作废（禁止只标直接下游）
                          ↓
                    拓扑序重算调度（依赖先算；默认全量，增量白名单制）
                          ↓
                registry 写入门卫：旧进程按旧哈希回写 → 拒绝入库（A4）
```

- **语义化参数哈希**：改注释/键序 → 哈希不变；plot 作用域改动不触发 data/model 重算（A5）
- **原子发布**：tmp + rename；半成品文件不被认作完成（A16）
- **写回生成新版本节点**：禁止原地覆盖，DAG 永保无环（A2）
- **predicate 词汇表状态**：fresh/stale/superseded/experiment/final/non_deterministic（D27）

### 2. C++ 确定性重算（算力优势 + 可复现的铁律结合）

- **确定性三件套**：固定分块 + pairwise 归约 + counter-based RNG（splitmix64，随机流只依赖迭代序号）
- **实测**：π 估计 100 万次迭代，2/8/16 线程 × 3 次运行**逐位一致**（3.1432440000000001）
- **跨语言对照**：预生成随机数文件喂两边 + KS 分布比较；`struct.pack` 用 `=` 前缀
- **双精度纪律**：结果 JSON 用 `%.17g`，解析端逐字节恢复 double（ULP=0）
- **回退**：每 C++ 任务必须有 Python 实现，失败 ≤2 次自动回退（A11：主控停摆=0）

### 3. 假设官（假设生命周期四步）

```
生成登记 → 必要性审查 → 主动挑战（反事实+实验设计强制） → 敏感度实验 → frozen
  │            │                │（Devil's Advocate）           │（关键假设强制）
  └── 作者回避：提交者不得参与审查/挑战/判定（系统强制，A18）
```
- **隐式假设映射表**：选最小二乘自动带出「误差正态/同方差/线性」等隐式假设，漏登记即打回（A17）
- **三级分类**：关键（结论依赖→强制敏感度）/ 重要（写入 limitations）/ 边缘（仅登记）
- **防形式化**：空话挑战被拒（反事实 <10 字符 = 未挑战）

### 4. 数值验证门禁（四断言 + 可复现性断言）

随机采样代入 / 量纲维度 / 边界条件（≥3 组）/ sympy 符号比对 + **可复现性断言**（同输入重跑 3 次哈希全等且与注册值一致）。挂 build 钩子，移除全部 Agent 后门禁仍可执行（A32）。

### 5. 双状态机（断点恢复 × 72h 时间窗）

- **进度状态机 = 唯一事实源**：checkpoint + append-only 变更日志；恢复第一步重放日志重建 dirty 全集，**完成标记一律不信，只信任哈希匹配的产物**（A13）
- **时间窗状态机只读不拥有**：商议 14% / 双轨迹 42% / 成文 25% 硬保底 / 纠错 ≤20%；预授权降级清单（重算挤占成文 >30% → 自动双轨迹转单轨迹等，不等用户确认）
- **铁律优先级**：正确性 > 完整性 > 时间窗

### 6. 排版层（直观性）

- **图清单**：六要素登记（含 data_source_hash——数据作废→图条目 dirty→门禁阻断）；info_gain 防 chart junk；孤儿图阻断（A22）
- **公式清单**：编号连续 + 引用完整，孤儿公式 blocking
- **密度标准**：O 奖语料 IQR 基线（默认先验：12 页 8-14 图 / 15 页 10-18 图；公式按题型分档）
- **直观性**：枚举化规则检查（轴标签/图例/单位/字号/子图上限）+ Qwen 视觉官 3 秒理解度（LLM 部分，由管线编排时调用）

### 7. 反投毒摄入门禁（S-1 · V6.1）

**核心命题：文本层 ≠ 可见内容。** PDF 允许文字存在但完全不渲染（渲染模式 `Tr=3`），
或用与背景相同的颜色——所有解析工具默认"文本层即真相"，会**原样读出隐藏指令**。

**四核心机制**（少一个就误报或漏报）：

| # | 机制 | 作用 |
|---|---|---|
| 1 | **三通道交叉验证** | B 原始直读 ∥ A 物理净化 → **差集 = 投毒候选（差异即证据）**；C 语义评分 |
| 2 | **像素交叉验证** | 文本层说"这里有字" → 去该 bbox 采样渲染像素，**真有笔画吗**？无 → 双证据确认不可见 |
| 3 | **双轨判定** | **物理隐藏**（白字/Tr3/离屏/小字/零宽）本身即异常独立告警；**语义可疑**需内容分 ≥2。元数据/书签等『隐藏载体』不算物理隐藏（否则 `format=PDF 1.7` 也告警） |
| 4 | **纯度判据** | 区分「隐藏文字」与「图表」：孤立像素/同档总数 **≥0.80** + 字符连通域 **6-200 个**（实测真隐藏字 96.8% vs 官方图表 29%/46%） |

**检测矩阵 8 类 30 项**：视觉隐藏（对比度/Tr=3/透明度/字号/离屏/遮挡）· 载体隐藏
（注释/元数据/XMP/书签/图层/表单/**附件内容**/JS）· 字符级（零宽/方向控制/同形字）·
结构级（原始字节流/增量更新）· 图片级（真实背景色/四档色差/**形态学去噪**/纯度/字符形态）·
数据集（公式注入/隐藏 sheet·行·列/**白字**/**批注**/定义名称/**宏**/超链接）· 语义级（多特征加权）·
跨文件（规划中）。

**两个必须记住的实现要点**：① 主数据源用 **`get_texttrace()`**——`type` 字段 = render mode，
**是唯一能拿到 `Tr=3` 的路径**（`get_text("dict")` 拿不到）；② 语义识别**必须多特征加权，不能单正则**
（`ignore all instructions` 匹配不到，放宽又会误判正文 `ignore the outliers`；模型术语与输出动词**须齐备**才计分）。

**实测**：攻击样本 16 项检出 15（94%）· 官方真实语料 41 文件**零误报** · 零 API 成本。
**赛期决定风险窗口**：赛中赛题受保护、无第三方整理版 → 风险 ≈0；**赛后"全套题目+优秀论文"包涌现时才是风险期**。

## 验收状态（2026-08-15 自测：32/32 全过；2026-09-25 新增反投毒 26/26）

| 判据 | 状态 | 实测 |
|---|---|---|
| A1 传递闭包作废 | ✅ | 闭包含全部下游（含跨层） |
| A2 环检测 | ✅ | A→B→A 100% 拦截 |
| A4 旧进程回灌拦截 | ✅ | 声明哈希 ≠ 生效哈希 → 拒绝 |
| A5 参数语义化哈希 | ✅ | 注释/键序不变哈希；作用域隔离 |
| A8 C++ 确定性 | ✅ | 2/8/16 线程×3 次逐位一致 |
| A13/A15/A16 恢复与原子性 | ✅ | dirty 集一致 / 全绿才推进 / 半成品防认 |
| A17/A18 假设官 | ✅ | 隐式假设漏登记检出 / 作者回避+防形式化 |
| A19/A22 排版隔离 | ✅ | experiment 禁入引用池 / 孤儿图 100% 检出；+模板门禁（官方清单）+篇幅门禁（≥75%上限） |
| A20 语料基线 | ✅ | 35 篇 O 奖：页中位 26 / 图 16 / 表 5 |
| A23/A24/A28 种子/可视化/审计 | ✅ | 17 条入库+检出 / diff 一致 / 四类违规全检出 |
| A26/A32 门禁 | ✅ | 四断言正误判定 / build 钩子纯脚本 |
| DA 独立角色 | ✅ | 实质挑战≥2 + 关键全覆盖 + 核实分离 |
| 反思器 | ✅ | 完成度检查 + ≤3 轮耗尽语义 |
| 跨语言对照 KS | ✅ | 同分布不拒 / 位移分布拒绝 |
| 编排层完整流程 | ✅ | v5.2：30 次调用预估准确；参谋组→拍板者(新开DS)→双轨迹→投票→修复回路→回归门 dry-run 全跑通 |
| **G1-G8 反投毒门禁** | ✅ | **26/26**：攻击样本检出 14/14（Tr=3/白字/浅灰/小字/离屏/中英文注释/隐形注释/书签/元数据×3/附件）· 正常图片零误报 · 隐藏字图片检出 · fail-closed 阻断 · 三件套+哈希链 · 数据集（公式注入/零宽/隐藏 sheet/隐藏行） |

其余判据（A3/A7/A9-A12/A21/A25/A27/A29-A31）依赖真实竞赛运行或多模型 API 实战，随实战累积。**真实 API 运行前必须先报预估调用次数**（完整场次 ≈30 次）。

## 铁律（本目录所有代码遵守）

**v5.2 六条（最高优先级）**：① 定稿即法律——S1.5 定稿是唯一真源，后续阶段不得自创方法/参数/口径 ② 单一责任人——每问求解、每个数字、每段正文有且仅有一个责任人 ③ 数字唯一入口——论文数字只从 registry 注入，writer 禁止生成数字/文献/声明 ④ 修一处→全链重做（freshness 含定稿节点） ⑤ 产物分数进门禁——不过 S7 不出管线 ⑥ 零上下文隔离——参谋/拍板/四评/写作各实例互不带上下文（防路径固执与污染）。

**v5.2.1 四条（2026-08-16 华数杯 C 四轮评审教训，堵"定稿时拍板、实现时打折"）**：
⑦ **实现-定稿比对门禁（S2.5）**——求解完成后 `impl_manifest.json` 逐条声明每个 D-决策的实现架构；与定稿不一致且未引用兜底条款编号 = 阻断（作废重做协议）。降级必须走决策日志，禁止静默打折。
⑧ **装配层归因纪律（R1-R6）**——对照表→结论直接导出（禁止反向圆说）；头条结论与结构性前提同级呈现；定义恒等式禁止包装为命题；可疑精确结果必须附机制；方法对比必须含数据形状适配基线。
⑨ **决策核心适配论证**——参谋提交方案前必须回答「数据形状→建模粒度」与「数据字典条款→模型口径映射」两项论证，缺一废票；拍板者定稿必须含口径条款清单与兜底协议编号。
⑩ **评审 v2.9 四审计**——评委 skill 新增 D4 声明强度 / D5 可疑精确性 / M7 MCDA 稳健性 / M8 基线完备性（`skills/data-science/mathmodel-judge-perspective/`）。

**v6.1 一条（2026-09-25 用户指令）**：
⑪ **外部输入必须先过反投毒门（S-1）**——题目 PDF / 数据集 / 图片进入 S0 之前必须经
`ingest/` 门禁检测物理隐藏投毒；有 HIGH 级发现 = `fail-closed` 阻断，须人工确认后以
`strict=False` 放行。**投毒报告先给人看（严格反例），不自动吞掉**；净化版 `clean_problem.md`
是后续建模的**唯一**题目来源；哈希链（files/clean/findings/report 四哈希 + 阈值 + 版本号）
进 `ingest_manifest.json`，**阈值改动 = 结论变更，可追溯**。

**既有铁律**：写评分离 · 评审零上下文隔离 · 作者回避 · 确定性门禁不认模型挂 build 钩子 · 72h 时间窗成文 25% 硬保底 · 旧进程不杀并行对照 · T5 窗口内权重 0 · 正确性 > 完整性 > 时间窗

## 运行方式

```bash
# 验收自测（零 API 成本，全确定性）
cd pipeline && python3 tests/test_engine.py && python3 tests/test_stages.py

# V6.1 反投毒门禁自测（26 项，零成本）
python3 tests/test_anti_poison.py

# V6.1 反投毒门禁实战（题目+数据集 → 三件套）
python3 ingest/anti_poison_gate.py <题目.pdf> <附件目录> --out ingest_out
#   → ingest_out/clean_problem.md / poison_report.md / ingest_manifest.json
#   有 HIGH 则退出码语义=阻断；人工确认后加 --no-strict 放行

# 语料基线（A20）
python3 baseline/corpus_baseline.py --corpus <O奖PDF目录> --n 35 --out baseline/baseline.json

# C++ 模板实测
g++ -O3 -fopenmp -std=c++17 cpp/templates/hot_loop_template.cpp -o /tmp/hot_loop
echo '{"n_iter": 1000000, "seed": 42}' | OMP_NUM_THREADS=8 /tmp/hot_loop
```

LLM 环节（四建模手/评审/假设官判定/排版视觉检查）由管线编排层按设计文档 §8 角色矩阵调用，本目录不包含（避免测试时消耗 API）。
